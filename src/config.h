/* Configuration */

#define SM_N960F // The only Note 9 I tested is the SM-N960F running LineageOS 20 but OneUI should be the same because both use the same Samsung kernel and the S-Pen works perfectly on LineageOS
